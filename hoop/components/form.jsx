import InputText from "./TextInput"

export const renderInputText = (style,placeholder,placeholderTextColor)=>{
    return (
        <InputText style={style} placeholder={placeholder} placeholderTextColor={placeholderTextColor} />
    )

}